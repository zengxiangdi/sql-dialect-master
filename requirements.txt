# SQL Dialect Master - Legacy dependency list
#
# The canonical package metadata lives in pyproject.toml.
# For normal installation use: python -m pip install -e .

sqlglot>=25.0.0
streamlit>=1.38.0
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.0.0

# Optional: for better Chinese tokenization in NL2SQL
# jieba>=0.42.1

# Development/test dependencies are declared in pyproject.toml as [project.optional-dependencies].dev
# Install with: python -m pip install -e ".[dev]"
